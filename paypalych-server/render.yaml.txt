services:
  - type: web
    name: paypalych-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PAYPALYCH_SECRET
        sync: false
